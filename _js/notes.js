// notes js
"use strict" //суворий режим
async / defer // неблокуючий шлях до скрипта
parseFloat // ислове значення
parseInt // ціле числове значення
typeof //  визначення типу даних

   console.log('') // вивід даних в консоль
alert // вивід інформації у спливаючому інформаційному вікні
document.write('') // вивід даних в документ html
prompt // вивід спливаючого вікна з вводом даних

const min = 1, max = 100 //випадкове число (мінімальне та максимальне значення)
const randomNumber = min + Math.floor(Math.random() * (max - min + 1)) //випадкове число (вивід)

